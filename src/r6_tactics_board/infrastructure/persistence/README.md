# Persistence

这里放工程持久化能力，负责把工程模型和磁盘文件互相转换。

## 适合放这里的内容

- `*.r6tb.json` 的加载、保存、格式兼容。
- 落盘前后的结构整理。
- 持久化错误的统一抛出和包装。

## 不要放这里的内容

- 文件打开对话框。
- 保存成功后的界面提示。
- 打开工程后如何初始化页面状态。

## 当前文件

- `project_store.py`: `*.r6tb.json` 的加载与保存。

## 落点规则

- “文件怎么存”放这里。
- “存之前要从哪些服务拿数据”放 `application/services/`。
