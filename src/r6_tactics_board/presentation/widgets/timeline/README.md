# Timeline Widgets

这里放时间轴相关控件，负责把时间轴数据呈现成可编辑 Qt 组件。

## 当前文件

- `timeline_widget.py`: 关键帧表格式时间轴。

## 适合放这里的内容

- 时间轴表格显示。
- 鼠标、键盘、选区等 UI 交互。
- 与页面之间的信号和事件接口。

## 不要放这里的内容

- 关键帧数据结构的核心变更算法。
- 撤销重做历史实现。
- 播放控制状态机。

## 落点规则

- 控件怎么显示和触发事件，放这里。
- 事件触发后数据怎么改，优先委托给 `application/timeline/`。
