# Editor Widgets

这里放编辑页专用控件，重点是可以独立维护的面板和输入组件。

## 当前文件

- `editor_panels.py`: 属性侧栏、楼层浮层、播放浮层。

## 适合放这里的内容

- 属性检查器。
- 楼层切换面板。
- 播放浮层和编辑页局部工具条。

## 不要放这里的内容

- 编辑页整体状态编排。
- 关键帧纯数据编辑。
- 地图画布图元。

## 落点规则

- 控件只要还能通过明确输入输出复用，就放这里。
- 一旦控件里开始塞大量页面流程判断，考虑把流程留给 `editor_page.py`，这里只保留 UI。
